# The following is the order of the links in the header of the website.
#
# Changing the layout file or adding/removing pages might require updating
# the `title:` in the _data/ui-text.yml file.

main:
  - title: "Publications"
    url: /publications/

  - title: "CV"
    url: /cv/
